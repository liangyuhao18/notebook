本文全面调查了NGSO通信系统的各个方面，从物理层到应用层以及整体结构设计愿景。  
## 物理层技术与无线接入方案  
### 天线系统
主要使用多波束天线和相控阵天线，它们提供大量高增益小波束，从而增加整个覆盖区域的系统容量。

### 链路预算[^1]
如下图，LEO链路在手持终端处实现了更好的CNR(载噪比)，对于VSAT终端，GSO和LEO链路都表现良好，但LEO链路在上行链路中仍比GSO链路高出多达16dB的CNR。LEO链路的显著优势是极低的RTD。
![[NGSO and GSO link budget.png|360]]
应当注意到NGSO卫星的移动性导致地面终端接收功率可变，高移动性还会引起多普勒效应。多普勒效应会产生时变频率偏移，从而使信道估计过程复杂化并增加对高信道估计开销的需求。

[^1]:**链路预算（Link Budget）**是在一个通信系统中对发送端、通信链路、传播环境（如大气、同轴电缆、波导、光纤等）和接收端中所有增益和衰减的核算。

### 星间链路(ISL, Inter-Satellite Link)
两种方式，射频无线电(RF)或激光。
#### 射频无线电
* (非太赫兹频段，THz) 具有技术成熟的优势，无需严格的捕获和跟踪功能，但与光媒介相比，它容易受到干扰并提供较低的数据速率。
* [[太赫兹（THz）频段通信]]，无需严格的捕获和跟踪功能，还有高传输速率。但要攻克半导体技术，比如生产高功率太赫兹发射机极其困难，且当前的太赫兹接收机容易产生更高的噪声系数。

#### 自由空间光通信
* 数据速率更高、尺寸更小、功耗更低的优势，但需要更复杂的捕获和跟踪功能。
* 目前在星链上使用

### 无线接入与复用
通常分为随机接入(RA)方案，即无协调多址协议和协调接入方案。

#### 随机接入
即ALOHA[^2]协议家族，如下表。

| 协议名称      | 核心思想     | 最大吞吐量     | 同步要求     | 复杂度 | 典型应用场景        |
| --------- | -------- | --------- | -------- | --- | ------------- |
| 纯 ALOHA   | 任何时刻发送   | ~18.4%    | 无        | 极低  | RFID、极轻负载传感器  |
| 时隙 ALOHA  | 时隙边界发送   | ~36.8%    | 时隙同步     | 低   | GSO 卫星、NB-IoT |
| 预约 ALOHA  | 先预约再发送   | 60%-80%   | 帧同步      | 中   | 卫星宽带、工业无线     |
| PRMA      | 语音激活预约   | 70%-90%   | 帧同步      | 中   | 卫星移动通信、语音业务   |
| 捕获 ALOHA  | 利用功率差异捕获 | 40%-60%   | 无 / 时隙同步 | 低   | 所有卫星系统        |
| MUD-ALOHA | 冲突中恢复多用户 | 100%-300% | 时隙同步     | 高   | 5G mMTC、大规模接入 |
| 编码 ALOHA  | 编码冗余抗冲突  | 80%-95%   | 无 / 时隙同步 | 中   | 短数据包物联网       |
| 异步 ALOHA  | 无需同步异步发送 | 30%-50%   | 无        | 中   | LEO 卫星互联网     |
[^2]:**完全无协调的随机发送**：终端任何时刻有数据产生，立即发送到共享信道。发送后等待一段时间，如果没有收到接收端的确认（ACK），就认为发生了冲突，等待一个随机的退避时间后重传。

#### 协调接入
**"先预约再发送，无冲突传输"**。通过中心节点（如基站）或预先约定的规则，为每个用户分配专属的无线资源（时间、频率、码道等），确保用户在自己的资源上传输时不会与其他用户冲突。  
典型范式如下：
- **频分多址（FDMA）** 将信道带宽划分为多个正交频率子信道，并将每个子信道分配给特定用户。
- **时分多址（TDMA）** 使用非重叠时隙来服务不同用户。
- **码分多址（CDMA）** 同时在相同时隙和频带上使用不同码字服务多个用户。
- **空分多址（SDMA）** 采用空间波束成形技术，在同一时间使用相同频带服务多个用户。用户间干扰可以通过定向波束成形来缓解。
- **非正交多址（NOMA）** 通过非正交资源同时允许多个用户接入。NOMA技术背后的基本原理是经典的多用户信息论。

***但是***上述所有接入方法在为大量用户提供服务时都存在局限性。特别是，RA方案可能导致过多碰撞，同样，协调接入管理大量用户所需的开销将使系统不堪重负。为解决此问题，**大规模接入**是一种新兴技术。

#### 卫星通信专用大规模接入技术
卫星通信的大规模接入面临比地面网络更严峻的挑战：**传播时延大（GSO 约 250ms，LEO 约 10ms）、多普勒频移大、用户分布极不均匀**。因此需要专门的优化技术：
#### 核心技术：
- **长前导码设计**：用于克服大的传播时延和多普勒频移
- **分波束接入**：将卫星覆盖区域划分为多个点波束，每个波束独立处理接入请求
- **动态资源分配**：根据不同波束的用户负载，动态调整接入资源的分配
- **异步免授权接入**：支持用户在不同的传播时延下异步接入，不需要精确同步