### 1. 部署目标

在 Proxmox VE 服务器（192.168.100.83，PVE 9.2.2）上部署三个虚拟机：
能在除家庭内网的外网环境中管理PVE物理机

| VM | 用途 | 系统 |
|----|------|------|
| VM 101 | NAS 文件存储（**珍贵老照片**），能进行外网访问管理 | 飞牛 OS（fnOS） |
| VM 102 | Minecraft 游戏服务器，能让外网用户加入游戏中来| Debian 13 |
| VM 103 | 轻量级 Web 应用测试服务器（网站测试 / 聊天机器人 / agentic 应用） | Debian 13 |

### 2. 宿主机硬件

| 项目 | 配置 |
|------|------|
| 主板 | 华擎 B450M Pro4-F（4× SATA3、M2_1 NVMe、M2_2 SATA M.2、支持 IOMMU） |
| CPU | AMD Ryzen 5 3400G（4核8线程，Vega 11 核显可直通） |
| 内存 | 16GB DDR4 3200MHz + 8GB swap |
| 系统盘 | 256GB 致钛 ZHITAI PC005（NVMe，M2_1，PVE 所在） |
| 数据盘 | 512GB Samsung 860 EVO（SATA SSD，**未使用**，规划给 NAS） |
| 备份盘 | 2TB WD Elements 2.5寸移动机械硬盘（USB，每周本地备份） |


### 3. 总体设计

一台物理机（PVE 9.2.2）运行三个相互隔离的虚拟机：NAS（VM 101）、Minecraft（VM 102）、Web 测试（VM 103）。所有虚拟机通过同一张虚拟网桥接入家庭内网，各自获得独立 IP；外网访问按“管理走 VPN、服务走端口映射 + 反向代理”的原则分层开放，尽量不把管理端口直接暴露到公网。

### 4. 网络架构

#### 4.1 内网（192.168.100.0/24）

| 设备 | IP（建议） | 说明 |
|------|-----------|------|
| 家庭网关/路由器 | 192.168.100.1（示例） | 承担 DHCP、NAT、端口转发、DDNS |
| PVE 宿主机 | 192.168.100.83 | 静态 IP；Web 管理界面 `https://192.168.100.83:8006` |
| VM 101 fnOS | 192.168.100.101 | NAS 管理界面默认 5666，文件共享 SMB 445 |
| VM 102 Minecraft | 192.168.100.102 | 游戏端口 TCP 25565 |
| VM 103 Web 测试 | 192.168.100.103 | HTTP 80 / HTTPS 443 反向代理目标 |

宿主机安装时把物理网卡桥接为 `vmbr0`，三台 VM 的虚拟网卡（virtio-net）都挂在 `vmbr0` 上，等价于插在同一台交换机上：VM 之间、VM 与路由器之间都能互通，由家庭路由器统一分配和管理地址。建议在路由器上做 DHCP 地址绑定（MAC 绑定），保证每台 VM 的 IP 长期不变。

#### 4.2 外网访问

先确认宽带是否具备公网 IPv4（或 IPv6）：

- 有公网 IP：在路由器或宿主机配置 DDNS（花生壳 / No-IP / 阿里云等）固定一个动态域名，再按下面的表格做端口映射。
- 没有公网 IP（运营商 NAT 内网）：改用 Tailscale / WireGuard 组网，或 frp 内网穿透；此时多数端口映射由穿透工具替代。

| 用途 | 协议/端口 | 目标 | 开放方式 |
|------|-----------|------|----------|
| PVE 管理界面 8006、SSH 22 | TCP | PVE 宿主机 .83 | 仅经 VPN（Tailscale/WireGuard）访问，不直接暴露公网 |
| fnOS 管理界面 5666、SMB 445 | TCP | VM 101 .101 | 优先走 VPN；确需直连时开启强密码、登录限速和 IP 白名单 |
| Minecraft | TCP 25565 | VM 102 .102 | 路由器 DNAT 公网端口 → 192.168.100.102:25565 |
| 网站 / 聊天机器人测试 | TCP 80/443 | VM 103 .103 | 路由器 DNAT 到反向代理（Caddy/Nginx），用域名 + HTTPS 访问 |

### 5. 物理机资源分配

CPU 4 核 8 线程、内存 16GB，总量有限。分配原则是“每台 VM 留足、宿主机留余量”，不做内存超分：

| 对象 | vCPU | 内存 | 磁盘 |
|------|------|------|------|
| PVE 宿主机 | 保留约 1-2 线程 | 保留约 2GB + 8GB swap | 256GB 致钛 NVMe（系统盘） |
| VM 101 fnOS | 2 | 4GB | 系统盘 32GB（NVMe）+ 512GB Samsung 860 EVO 整盘直通 |
| VM 102 Minecraft | 2 | 4GB | 32GB（NVMe），世界变大可在线扩容 |
| VM 103 Web 测试 | 2 | 2GB | 40GB（NVMe），预留 Docker/模型文件空间 |
| 合计 | 6 / 8 线程 | 10 / 16GB | 系统盘合计约 104GB，NVMe 留约 130GB 余量 |

要点：

- 磁盘：PVE 装在 256GB 致钛 NVMe 上；102、103 的系统盘和 101 的系统盘都放在 NVMe 上保证速度。512GB 三星 SSD **不预先格式化**，在 PVE 中作为整盘直通设备挂给 VM 101，由 fnOS 自己建存储池，数据完整性和权限管理最可靠。
- CPU：VM 的 CPU 类型选 `host`，把宿主 CPU 特性透传给虚拟机；Minecraft 服务端吃单核性能，6 vCPU 分摊在 8 线程上，日常不超卖过多。
- 内存：不超分，10GB 分配 + 约 2GB 宿主机开销后，仍留余量给页面缓存和 I/O。
- 核显（Vega 11）保留给宿主机作显示输出和紧急控制台，本期不做 GPU 直通，降低复杂度和故障面。

### 6. 核心技术原理（简要）

- **KVM/QEMU 虚拟化**：AMD CPU 提供硬件虚拟化扩展（SVM），KVM 让虚拟机指令直接跑在 CPU 上，QEMU 负责模拟主板、网卡、磁盘等设备。相比纯软件模拟，性能接近物理机。
- **VirtIO 半虚拟化**：虚拟机的网卡和磁盘使用 virtio 驱动，宿主与虚拟机通过共享内存队列直接交换数据，省去逐条模拟硬件指令，吞吐高、CPU 占用低。
- **vmbr0 网桥**：本质是一个内核里的虚拟交换机。物理网卡和所有 VM 的虚拟网卡都作为端口接入，二层上完全等价于真实局域网，所以每台 VM 可以拥有独立的局域网 IP。
- **IOMMU 与设备直通（AMD-Vi）**：B450M 主板支持 IOMMU，可把真实硬件（SATA 控制器、USB 控制器、显卡）直接映射给某一台 VM，让虚拟机独占设备，性能接近原生。本项目只做磁盘整盘直通（raw device mapping），不直通整块 SATA 控制器，避免影响宿主其他 SATA 口。
- **LVM-thin 与快照**：PVE 的虚拟机磁盘默认建在 thin 池上，可以在 VM 运行中做一致性快照，用于升级、测试前的回滚点。
- **vzdump 备份**：PVE 自带的虚拟机备份工具，支持快照模式下在线备份，把 VM 打包压缩后写到本地目录；挂载到宿主机上的 2TB USB 移动硬盘就是每周备份的目标盘。
- **端口转发（DNAT）与反向代理**：路由器把公网端口映射到内网 VM；反向代理（Nginx/Caddy）再把 80/443 的请求按域名分发到 VM 103 内不同应用，并统一终结 HTTPS 证书。
- **VPN（Tailscale/WireGuard）**：在宿主机或路由器上建立加密隧道，管理员在任何外网环境接入后，就像回到家庭内网一样访问 8006、SSH、NAS 管理界面，而公网上看不到这些端口。

### 7. 为什么这么做（设计理由）

- **三台 VM 相互隔离**：NAS 里是珍贵老照片，Web 测试 VM 里可能跑来历不明的代码，Minecraft 服务器会被人群持续访问。分开部署后，任何一台崩溃、中毒或被攻击，都不影响另外两台；每台 VM 还能独立打快照、随时回滚。
- **NAS 数据盘整盘直通**：照片数据不应交给宿主文件系统再转一层。整盘直通后 fnOS 直接管理 512GB SSD，SMART 信息、文件系统、存储池都由 NAS 系统自己掌控，性能好、故障排查直观。
- **管理端口只走 VPN**：PVE 的 8006 和 SSH 直接暴露公网是风险最高的做法，很容易被扫描爆破。用 VPN 后外网管理 PVE 和 NAS 的安全性与便利性兼得；公网只开放必要服务端口。
- **Minecraft 单独开放 25565**：游戏服务器需要固定端口和公网可达，把它单独拆开，即使被恶意玩家干扰也不影响 NAS 数据；开服前打快照，出问题一键还原。
- **Web 测试走域名 + HTTPS 反向代理**：测试站点、聊天机器人适合用域名区分、TLS 加密，避免在公网上明文裸奔；反向代理还能统一加访问控制、限速和日志。
- **备份到 USB + 异地副本**：2TB 机械盘每周做整机备份（含 NAS VM），符合“3-2-1”原则中的本地两份；老照片建议再启用网盘/对象存储做异地副本，防止火灾、盗窃等本地灾难。
- **资源克制**：16GB 内存下不超分、不贪多，宿主机始终留有响应余量；Minecraft 需要时再扩内存，比一开始超卖导致全家卡顿更稳妥。

### 8. 实施步骤（摘要）

1. PVE 9.2.2 安装到 256GB 致钛 NVMe（M2_1），分区采用默认的 ext4/LVM-thin；安装时给宿主机设置静态 IP `192.168.100.83/24`、网关和 DNS。
2. 安装完成后确认 `vmbr0` 桥接网络正常，升级系统；在 BIOS/内核参数中启用 IOMMU（`amd_iommu=on iommu=pt`），为后续磁盘直通做准备。
3. 在 PVE 中上传 fnOS、Debian 13 安装 ISO；创建 VM 101/102/103，磁盘、内存按第 5 节配置，网卡选 virtio，CPU 类型选 `host`。
4. 为 VM 101 添加 512GB 三星 SSD 整盘直通（挂载为 scsi 设备，选择物理磁盘 `/dev/disk/by-id/...`），再安装 fnOS 并初始化存储池。
5. 依次安装 Debian 13 到 VM 102（Minecraft，推荐 PaperMC 服务端）和 VM 103（Web 测试，安装 Docker + Nginx/Caddy）。
6. 内网验证：三台 VM 都能访问互联网、互能 ping 通；Minecraft 在局域网内可加入；Web 应用可在内网打开。
7. 外网打通：路由器做 DDNS + 端口映射（25565 → .102，80/443 → .103）；宿主机/路由器配置 Tailscale 或 WireGuard；用 HTTPS 证书配置反向代理域名。
8. 备份与监控：把 2TB WD Elements 挂载到宿主机并配置为 PVE 备份存储，设置每周日 03:00 全量备份（保留 2-3 份）；fnOS 中配置老照片的异地备份任务。

### 9. 备份与安全清单

- PVE 配置与三台 VM 每周自动 vzdump 到 2TB USB 盘；定期抽查恢复演练（至少恢复一次 VM 确认备份可用）。
- 所有 VM 和 PVE 修改默认口令，SSH 禁止 root 密码登录；公网侧服务全部套 HTTPS。
- 路由器开启简单防火墙，只放行 25565、80、443；管理端口仅允许 VPN 网段访问。
- fnOS 开启 SMART 监控与定时磁盘自检；老照片再保留一份异地副本（网盘/对象存储）。
- 系统与软件每月更新；更新前对相关 VM 打快照。
